<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Intercambios" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azulPrincipal</item>
        <item name="colorSecondary">@color/dialog</item>
        <item name="colorOnPrimary">@color/azulPrincipal</item>
    </style>

    <style name="Theme.Intercambios" parent="Base.Theme.Intercambios" />

    <style name="Theme.Intercambios.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PickerTheme" parent="Base.Theme.Intercambios">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">80%</item>
    </style>
    
    <style name="CustomChip" parent="Widget.MaterialComponents.Chip.Entry">
       <item name="chipStrokeColor">@color/azulPrincipal</item>
        <item name="chipStrokeWidth">2dp</item>
        <item name="android:textSize">21sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="backgroundColor">@color/azulClaro</item>
    </style>

    <style name="CustomToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Intercambios.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Intercambios.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>